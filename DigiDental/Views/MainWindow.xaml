<Window x:Class="DigiDental.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vmb="clr-namespace:DigiDental.ViewModels.ViewModelBase"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="DigiDental" Height="600" Width="800" WindowState="Maximized">
    <Window.Resources>
        <vmb:RadioButtonConverter x:Key="radioButtonConverterKey"></vmb:RadioButtonConverter>
        <s:Boolean x:Key="true">true</s:Boolean>
        <s:Boolean x:Key="false">false</s:Boolean>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="檔案">
                    <MenuItem Header="離開" Click="MenuItem_Exit_Click"></MenuItem>
                </MenuItem>
                <MenuItem Header="設定" Click="MenuItem_Setting_Click"></MenuItem>
                <MenuItem Header="關於"></MenuItem>
            </Menu>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" Name="GridLife"></ColumnDefinition>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition Width="0.8*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" DockPanel.Dock="Top" Margin="5"  VerticalAlignment="Top" Content="病患搜尋" />
                    <StackPanel Grid.Row="1" Margin="5" VerticalAlignment="Top">
                        <Label DockPanel.Dock="Top" Margin="5,0,5,0" Content="病歷號碼"/>
                        <TextBox DockPanel.Dock="Top" Margin="5,0,5,0" Text="{Binding Patients.Patient_Number, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                        <Label DockPanel.Dock="Top" Margin="5,0,5,0" Content="姓名"/>
                        <TextBox DockPanel.Dock="Top" Margin="5,0,5,0" Text="{Binding Patients.Patient_Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                        <Label DockPanel.Dock="Top" Margin="5,0,5,0" Content="身分證號"/>
                        <TextBox DockPanel.Dock="Top" Margin="5,0,5,0" Text="{Binding Patients.Patient_IDNumber, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
                        <Label DockPanel.Dock="Top" Content="性別" Margin="5,0,5,0"/>
                        <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                            <RadioButton GroupName="Gender" Content="男" Margin="10,0,10,0" IsChecked="{Binding Patients.Patient_Gender, Converter={StaticResource ResourceKey=radioButtonConverterKey}, ConverterParameter={StaticResource true}}" />
                            <RadioButton GroupName="Gender" Content="女" Margin="10,0,10,0" IsChecked="{Binding Patients.Patient_Gender, Converter={StaticResource ResourceKey=radioButtonConverterKey}, ConverterParameter={StaticResource false}}" />
                        </StackPanel>
                        <Label DockPanel.Dock="Top" Content="生日" Margin="5,0,5,0"/>
                        <DatePicker DockPanel.Dock="Top" Margin="5,0,5,0" SelectedDate="{Binding Patients.Patient_Birth, UpdateSourceTrigger=PropertyChanged}" />
                        <Border DockPanel.Dock="Top" Margin="5" BorderBrush="Black" BorderThickness="1" Height="150" >
                            <Image Stretch="Fill" />
                        </Border>                        
                    </StackPanel>
                    <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Orientation="Horizontal">
                        <Button HorizontalAlignment="Right" Content="修改" />
                        <Button HorizontalAlignment="Right" Content="儲存" />
                        <Button HorizontalAlignment="Right" Content="取消" />
                    </StackPanel>
                </Grid>
                <Button Grid.Column="1" Click="Button_Stretch_Panel" />
                <TabControl Grid.Column="2" Name="FunctionsTab"></TabControl>
                <!--<TabControl Grid.Column="2" ItemsSource="{Binding Tabs}" SelectedItem="{Binding TabControlSelectedItem}" TabStripPlacement="Left">
                    <TabControl.Resources>
                        <DataTemplate DataType="{x:Type ucvm:ListFunctionViewModel}">
                            <vuc:ListFunction x:Name="lfuc"/>
                        </DataTemplate>
                        <DataTemplate DataType="{x:Type ucvm:TemplateFunctionViewModel}">
                            <vuc:TemplateFunction />
                        </DataTemplate>
                    </TabControl.Resources>
                    <TabControl.ItemTemplate>
                         --><!--this is the header template--><!--
                        <DataTemplate>
                            <ContentPresenter Content="{Binding Header}"
                                              TextBlock.FontFamily="Microsoft JhengHei" TextBlock.FontSize="16">
                                <ContentPresenter.LayoutTransform>
                                    <RotateTransform Angle="270"/>
                                </ContentPresenter.LayoutTransform>
                            </ContentPresenter>
                        </DataTemplate>
                    </TabControl.ItemTemplate>
                </TabControl>-->
            </Grid>
        </DockPanel>
    </Grid>
</Window>

